# Model artifacts — generated by infra/scripts/train_model.py
#
# Run:  python infra/scripts/train_model.py
#
# Files produced:
#   model_meta.json   — Ridge regression weights + normalization params (consumed by Rust at startup)
#
# This directory is committed so the Docker image picks up the model file.
# Re-run the training script after changing feature set or retraining on new data.
